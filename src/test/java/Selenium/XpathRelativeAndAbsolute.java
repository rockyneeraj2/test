package Selenium;

public class XpathRelativeAndAbsolute {
    public static void main(String[] args) {

        //Absolute xpath:
        // it contains the complete path from root element of page to desired element
        // Absolute xpath start with root node - single forward slash
        // Drawback of using Absolute xpath - any slight change in DOM make xpath invalid

        //Relative Xpath:
        //with relative xpath, the xpath start from mid of html DOM structure
        //it begin with double forward slash
        // it is less brittle
    }
}
