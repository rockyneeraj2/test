package Selenium;

public class Xpath_preceding {
    public static void main(String[] args) {

        //preceding: it selects all nodes that appear before the context (current) node
        //syntax://tagname[@attribute='value']//preceding::tagname

        //preceding-sibling: it selects all the nodes that have same parent as context (current node) appear before node
        //syntax://tagnae[@attribute='value']//preceding-sibling::tagname
    }
}
